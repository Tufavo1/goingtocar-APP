import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ProgramsFavorite.Tellevoapp',
  appName: 'TellevoApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
